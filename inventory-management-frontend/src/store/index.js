import { createStore } from "vuex";
import apiService from "./module/apiService";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    apiService,
  },
});
